<!DOCTYPE html>
<html>
<head>
  <title>Manage Users</title>
</head>
<body>
  <h1>All Users</h1>
  <table border="1">
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Role</th>
    </tr>
    <% users.forEach(u => { %>
      <tr>
        <td><%= u.id %></td>
        <td><%= u.name %></td>
        <td><%= u.role %></td>
      </tr>
    <% }) %>
  </table>
</body>
</html>
