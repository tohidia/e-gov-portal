
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Loan Request - E-Gov Portal</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans min-h-screen flex flex-col">

  <!-- Navbar -->
  <header class="bg-green-800 text-white py-4 shadow-md">
    <div class="max-w-5xl mx-auto flex justify-between items-center px-4">
      <h1 class="text-2xl font-semibold">E-Government Portal</h1>
      <nav class="space-x-4">
        <a href="/" class="hover:underline">Home</a>
        <a href="/services" class="hover:underline">Services</a>
        <a href="/services/license" class="hover:underline">License Renewal</a>
        <!-- <a href="/login" class="hover:underline">Login</a> -->
      </nav>
    </div>
  </header>

  <!-- Main Form -->
  <main class="flex-grow flex justify-center items-center py-10">
    <div class="bg-white w-full max-w-md rounded-xl shadow-lg p-8">
      <h2 class="text-2xl font-bold text-center text-green-700 mb-6">
        Loan Request Form
      </h2>

      <form id="loanForm" class="space-y-5">
        <div>
          <label class="block font-medium text-gray-700 mb-1">Full Name</label>
          <input type="text" id="fullName" placeholder="Enter your full name"
                 class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-green-500" />
        </div>

        <div>
          <label class="block font-medium text-gray-700 mb-1">Amount Requested</label>
          <input type="number" id="amount" placeholder="Enter amount (AFN)"
                 class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-green-500" />
        </div>

        <div>
          <label class="block font-medium text-gray-700 mb-1">Email Address</label>
          <input type="email" id="email" placeholder="you@example.com"
                 class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-green-500" />
        </div>

        <button type="submit"
                class="w-full bg-green-700 hover:bg-green-800 text-white font-semibold py-2 rounded-lg transition">
          Submit Request
        </button>
      </form>

      <p id="message" class="mt-4 text-center font-medium"></p>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-green-800 text-white py-4 text-center mt-auto">
    <p>© 2025 E-Gov Portal — All Rights Reserved.</p>
  </footer>

  <!-- JavaScript -->
  <script>
    const form = document.getElementById("loanForm");
    const message = document.getElementById("message");

    form.addEventListener("submit", (e) => {
      e.preventDefault();

      const fullName = document.getElementById("fullName").value.trim();
      const amount = document.getElementById("amount").value.trim();
      const email = document.getElementById("email").value.trim();

      if (!fullName || !amount || !email) {
        message.textContent = "❌ Oops! Full Name, Amount, and Email are required.";
        message.className = "mt-4 text-center font-medium text-red-600";
      } else {
        message.textContent = "✅ Loan request submitted successfully!";
        message.className = "mt-4 text-center font-medium text-green-600";
        form.reset();
      }
    });
  </script>

</body>
</html>
