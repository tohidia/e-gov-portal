<div class="container mt-5">
    <h2>Payment for Request #<%= requestId %></h2>
    <p>Amount: $<%= amount %></p>

    <form method="POST">
        <button type="submit" class="btn btn-success">Pay Now (Simulated)</button>
    </form>
</div>
